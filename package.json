{
  "name": "aarkme",
  "version": "1.0.0",
  "private": true,
  "description": "aarkme is a compact, curated profile for your favorite movies, albums, books, and games.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexandredoamarante/aarkme.git"
  },
  "license": "ISC",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/browser-chromium": "^1.60.0"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "@supabase/supabase-js": "^2.106.2"
  }
}
